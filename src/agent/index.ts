/**
 * BunShell Agent System — Layer 5
 *
 * Subprocess-isolated agent execution with capability injection.
 *
 * @module
 */

export type { AgentConfig, AgentResult } from "./types";
export { runAgent } from "./sandbox";
